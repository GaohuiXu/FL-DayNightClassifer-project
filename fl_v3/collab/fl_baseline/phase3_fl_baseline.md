# Phase 3 — FL baseline + tail-collapse investigation

> Consolidated record for the clean federated camera+LiDAR BEV/3D-detection baseline that federates the
> locked centralized **bb02d** (0.5656 mAP / 0.5733 NDS) on a healthy 25-client geographic non-IID partition
> of nuScenes trainval. The honest FedAvg baseline lands at **0.247 mAP** (R=15); the gap to centralized is
> the non-IID **tail** (head classes retain 66–87%, tail classes detect-but-don't-calibrate, ~0 AP at high
> recall). A read-only gradient/module teardown and a balanced head-retrain (cRT) probe both point away from
> a head-only fix and toward a representation-level cause **under this recipe/probe** — leaving the path to
> ≥0.50 mAP open (status: PAUSED at the owner's path-to-≥0.50 decision).

---

## Setup

### Partition (25-client geographic non-IID) — health check: GO

Generated by `fl_v3/scripts/p3_partition_health.py` on the **`info_cache_msweep10` trainval `train`** cache —
the exact data the FL run federates. Machine-readable: `partition_health.json` (this dir).

**Config (the EXACT FL partition):** `partition-mode=log_group`, floor (`min-keyframes-per-client`) = **400**,
`nuscenes-num-clients` = **25**, seed **20259** (partition-seed `""` → falls back to run seed 20259). Cache hash
`cb9818e73c1d…`. Matches what `t4_reference.json` / `p1_bb02d.json` declare ⇒ satisfies the D10 readiness contract.

**Totals + N-derivation (non-arbitrary floor):** 50 logs, 28,130 keyframes, 944,881 boxes. Logs by location:
boston-seaport 22, singapore-onenorth 17, singapore-queenstown 7, singapore-hollandvillage 4. `n_max@floor400 = 39`
⇒ requested N=25 is honored (LPT-balanced into 25 location-coherent groups). Floor band: 200→44, 400→39, 600→29
(floor is not on a cliff).

**Health gates (all PASS):**

| gate | result |
|---|---|
| Determinism (re-build → identical shards) | PASS |
| Fair-comparison anchor (∪ clients == pooled train, 28,130) | PASS |
| No cross-client overlap | PASS (0) |
| Whole-log integrity (no log split; all 50 logs assigned) | PASS |
| No starved client (< floor 400) | PASS (min 812) |
| N == requested (25) | PASS |

The union of the 25 clients **equals** the centralized pooled-25-client trainval set bb02d trained on ⇒
**FL-vs-centralized is the same data** (the fairness precondition).

**Per-client volume + continuity:** keyframes/client min 812 / max 1449 / mean 1125 / CoV 0.153 / max-min ratio
1.78. Each client is 1–4 whole contiguous logs (20–36 scenes) = real driving sessions. Low volume CoV ⇒
num-examples (keyframe) FedAvg weighting ≈ uniform; no single client dominates.

**Non-IID profile (the desired realism):**
- *Geographic density axis (dominant):* Boston (15 clients) dense (~32–58 obj/kf); Singapore (10 clients:
  onenorth 5, queenstown 3, hollandvillage 2) sparse (~14–31 obj/kf). Singapore clients carry the largest
  JSD-to-global (up to 0.143).
- *Label-distribution skew (per-class CoV of per-client counts):* car 0.42 (most uniform), pedestrian 0.61,
  truck 0.60, bicycle 0.59, bus 0.54, motorcycle 0.68, construction_vehicle 0.77, traffic_cone 0.80, barrier
  0.85, **trailer 1.09 (most skewed)**.
- *JSD-to-global (class distribution):* mean 0.053 bits, min 0.010, max 0.143 ⇒ moderate label skew (not
  pathological).
- *Weather/time axis (from `scene.json`):* night clients 3 (15/16/24, all Singapore), rain clients 9, pure
  day-dry 15 — a natural domain-shift axis for the later attack/defense story.

**Global (pooled) class distribution — the difficulty map:** car 43.7%, pedestrian 19.7%, barrier 13.2%,
traffic_cone 8.7%, truck 7.7%, trailer 2.2%, bus 1.4%, construction_vehicle 1.3%, motorcycle 1.1%, bicycle 1.0%.
The long tail (trailer/bus/CV/moto/bicycle, each ~1–2%) is exactly the set bb02d is weakest on centrally — and
the most non-IID across clients.

**Degenerate-class note (a NOTE, not a no-go):** 6 clients have zero instances of a class — trailer absent from
clients 15,17,18,22,23,24; client 24 also has zero construction_vehicle (all Singapore; trailer is
Boston-concentrated). This is genuine geographic rarity, not degeneracy: every client has all head classes
(car/ped/barrier/cone/truck), the shared CenterPoint heatmap head still receives trailer/CV gradients from the
19 clients that have them, and FedAvg propagates trailer features globally. The skew is **preserved** (not
IID-ified); flagged so per-class FL retention is read with a small expected extra FL hit on trailer/CV.

**Decision: GO.** No partition fix needed; the partition is deterministic and D10-compliant, fit to host the
clean FL reference T5 binds to.

### FL recipe (FedAdam/server-opt, D17) + fairness anchor

The tuned recipe to federate locked bb02d into a clean bf16 FL baseline, designed via a 4-recipe judge-panel
(workflow `wf_1e63db64`). Config: `fl_v3/configs/fl_bb02d_fedadam.json`. The hyperparameters below are the
**starting** recipe (intended to be tuned on a cheap probe sweep before the full R=30 run).

**What is federated:** the FULL bb02d model — **270 trainable tensors / 33.17M params, ZERO frozen**
(camera_backbone 169 *trained* + lidar_backbone 39 + 62 non-backbone). D17 amends D1: the Swin-T backbone is
trained, so the old "exclude+reconstruct the frozen backbone" optimization (DT3-A) no longer applies — the whole
model is the FL update vector. (bb02d's BEV aug + img-flip + per-class heatmap weights flow to clients via the
`_aug_from_run` loader hook + `build_criterion`; CBGS/GT-paste are centralized-only.)

**The recipe:**

| knob | value | why (FL ≠ centralized) |
|---|---|---|
| Local epochs E | **1** | All 4 judges: more E ⇒ more client drift under non-IID on a 33M fine-tune. E=2 is a compute-headroom variant only. |
| Rounds R | **30** (D17 ≥30); also report **R=15** | R=15 = 15 data-passes = PARITY with centralized; R=30 = the D17 reference. |
| Client optimizer | **AdamW**, wd 0.01 | Match bb02d's optimizer. Adam-family only (reproduction-fidelity decision — no SGD). |
| Client LR | base **1e-3**, **global cosine over rounds**, **3-round warmup**, floor 0.05× | No single trajectory in FL ⇒ a per-round global schedule (server broadcasts the round's LR); bb02d's cold-start needs warmup. |
| grad-clip | **35** (NOT 1.0) | Match centralized. Over-tight clip (1.0) on the 25-client "huge-batch" aggregate ⇒ under-convergence. |
| backbone-lr-mult | **0.1** | The heavy Swin-T trains at 0.1× the head LR (the centralized lever; stabilizes the trained backbone in FL). |
| Server optimizer | **FedAdam** (D17), η=**0.01**, β1 0.9, β2 0.99, **τ 0.01**, **5-round η-warmup** | The retention lever (server momentum vs non-IID dilution). η-warmup + larger τ guard the early-round FedAdam blow-up (un-warmed step ≈ η·sign(Δ) before v̂ stabilizes). |
| Server EMA | decay **0.9** over rounds | Fair to the EMA'd centralized anchor (0.5656 is an EMA ckpt). Snapshot BOTH raw + EMA; report the **max**. |
| fraction-fit | **1.0** (all 25/round) | REQUIRED by D10 for a valid reference checkpoint. |
| Snapshots | rounds 10,15,20,25,30 (+ema) | Centralized peaked mid-run (ep15, not ep20) — pick the PEAK round post-hoc. |

Server optimizer is an axis **orthogonal to `defense-type`** — `defense-type` stays `none` (D10/T5 contract
intact); FedAdam composes with any defense for the later benchmark.

**Fairness anchor (basis = equal full-data-equivalent passes):** centralized bb02d = 15 passes (15 epochs over
the 28,130-kf pooled train). With full participation and ∪(25 clients) == the pooled set, 1 round (E=1) = 1
data-pass ⇒ FL = R passes. Report two points:
- **R=15 (15 passes) = PARITY** — the apples-to-apples head-to-head with centralized 0.5656 (the honest
  headline; the gap = the FL dilution tax).
- **R=30 (30 passes, 2×) = the D17 reference** — FL is allowed more rounds because the FL optimization (33M
  update vector + non-IID drift) is harder than centralized SGD over the pooled set, and aggregation
  regularizes (centralized overfits past ep15). Report both; do not lead with the 2×-pass number alone.

The binding downstream floor: **FL ≥ 0.50 mAP** after the 25-client non-IID dilution (a recipe+partition
retention target, from centralized 0.5656).

**Tuning plan (cheap probe before the full run)** — the FedAdam server-LR × client-LR interaction is the #1
divergence risk; tune on short runs (`run_fl_bb02d_a100.sh` with
`ROUNDS=6 EVAL_MODE=every_n EVAL_LIMIT=256 SERVER_LR=.. CLIENT_LR=.. TAG=..`):
1. Step 0 — measure (R=2, base config): real per-round wall-time + trains-clean (no NaN) + VRAM fit.
2. Primary 1-D sweep (η-warmup ON): server-lr ∈ {0.003, 0.01, 0.03} at client-lr 1e-3, R=6 — rank by the
   round-6 proxy (car-recall + eval-loss slope); pick the highest non-diverging η.
3. Client-lr check: client-lr ∈ {5e-4, 1e-3, 2e-3} at the chosen η, R=6.
4. τ check (only if under-converging): τ 1e-2 → 1e-3.
5. Monitor rare classes (trailer/CV); if they collapse, consider class-balanced client weighting.

Then the winning (η, client-lr, τ) → the full **R=30** run; **≥3 seeds (mean±std)** for the final claim (D16).

**Code built (all default-off ⇒ byte-identical baseline; 297 tests pass + CPU flwr smoke green):**
- `strategy/server_opt.py` — the FedOpt server optimizer (FedAdam / FedAvgM / identity FedAvg) with η-warmup;
  pure-numpy, fp64, deterministic. `fedavg`+η=1 is a true identity (byte-identical baseline).
- Wired into BOTH the production Flower strategy (`flower_strategies._aggregate_with_core`) and the in-process
  `engine/local_runner.py` — Δ = aggregate − global is form-agnostic ⇒ FedAdam composes with any defense.
- Per-round global client-LR schedule (`flower_strategies.configure_train`) + server-side cross-round EMA +
  per-round snapshot checkpoints (`server_app`).
- Client recipe (`training/loop.train_local`): backbone-LR-mult param-groups + grad-clip + AdamW, wired through
  `client_app`/`local_runner` (fires only when the backbone is trained ⇒ frozen/dummy stays the byte-identical path).
- STEP 4 slice-map: `BB02D_TRAINABLE_MODULE_SLICE_MAP` (270) + config-conditional `assert_trainable_layout`.
- pyproject registration: all bb02d model + recipe knobs added to `[tool.flwr.app.config]`; `det-class-weights`
  is a comma-STRING (flwr rejects arrays), uniform→None keeps the default loss byte-identical.
- Provenance records an honest `fl_recipe` block (D10 validation unchanged, FedAdam stays D10-compliant with
  defense=none). Launcher `scripts/run_fl_bb02d_a100.sh` (A100:4, msweep10 cache, writes D10 provenance into
  every snapshot dir, probe overrides via env).
- Tests: `test_server_opt.py` (12), `test_fl_server_opt_integration.py` (5), `test_fl_config_keys_registered.py`
  (4), bb02d-layout (1). CPU `flwr` smoke validated the full Flower path (FedAdam + cosine LR + EMA + snapshots)
  end-to-end.

### Runtime profile + speed-up (R1, before the heavy runs)

**Single-GPU per-client teardown** on client-0's real shard (1075 kf), A100-SXM4-40GB, batch-4 (the FL
per-client batch), bf16. Tool: `p1_profile_a100.py --opt-compare` (job 6778238, 6:17). Raw: `p3_fl_profile.json`.
Baseline = un-optimized FL config (sdpa on, compile off):

| stage | mean ms | share | note |
|---|---:|---:|---|
| dataloader | 0.25 | 0.1% | NOT loader-bound at batch-4 / 4 workers |
| forward_total | 120.6 | 26% | |
| — lidar_backbone | 36.4 | 7.8% | the dense 2D LiDAR trunk — biggest single forward module |
| — camera_backbone (Swin-T) | 31.2 | 6.7% | |
| — lidar_encoder (PFN) | 16.7 | 3.6% | |
| — fusion | 15.8 | 3.4% | |
| — bev_neck / view_transform / head / camera_neck / preprocess | ~20 | 4% | |
| **backward** | **214.8** | **46%** | the dominant cost (1.8× the forward) |
| loss + optimizer | 11.0 | 2% | |
| **wall step** | **353** | — | util **95.5%** ⇒ compute-bound; peak VRAM 18.1 GB |

Top self-CUDA kernels: `aten::copy_` (LSS splat + bf16 casts), `native_group_norm` fwd+bwd,
`convolution_backward`, `cudnn_convolution` — the GroupNorm + conv BEV stack dominates.

**Single-GPU speed-up A/B (batch-4):**

| regime | step ms | speedup | util | peak VRAM |
|---|---:|---:|---:|---:|
| baseline (sdpa, no compile) | 353 | 1.00× | 95.5% | 18.1 GB |
| + compile (camera_backbone) + sdpa | 297 | 1.19× | 97.1% | 15.1 GB |
| **+ compile BEV-stack (camera_neck+fusion+bev_neck+head+lidar_backbone)** | **244** | **1.45×** | 96.0% | 15.5 GB |

The BEV-stack subgraphs have static shapes (512²/256² BEV) ⇒ no recompilation; the variable-pillar PFN + ragged
LSS splat stay eager. A `det-compile-bev` knob was added (registered, default off ⇒ byte-identical); the launcher
sets a persistent `TORCHINDUCTOR_CACHE_DIR`.

**FL-path reality (the Ray-run telemetry the single-GPU profiler can't see)** — two R=2 Ray runs (compile ON/OFF)
with `build_s`/`train_s`/`round-prof` exposed the real bottlenecks:
1. **Launcher `PYTHONPATH` bug (caught + fixed).** The shared `.venv_v3` editable-installs `fl_v3` from ONE
   worktree; the launcher didn't prepend this worktree's `src`, so a bare `flwr run` imported stale sibling code —
   the first 52-min R=2 silently ran plain FedAvg (no FedAdam/EMA/snapshots/recipe). Fix:
   `export PYTHONPATH=$REPO/fl_v3/src` in the launcher (propagates to forked Ray actors). Tell-tale: the re-run
   now logs `round-prof`, saves server-EMA + snapshots, emits `build_s`/`train_s`.
2. **`torch.compile` is NET-NEGATIVE in FL — disabled.** The model is rebuilt per (client, round), so compile
   re-traces every client: `train_s` = 262 s (compile) vs 97 s (no-compile) — the per-client compile cost is not
   amortized over a ~281-step epoch. It also breaks the update-vector key contract (compile adds an `_orig_mod.`
   prefix ⇒ "reply key order differs from global"). → `compile-backbone=false`, `det-compile-bev=false`. (The
   1.45× single-GPU win needs a persistent compiled model on the actor — a future optimization, not worth the
   risk for the first baseline.)
3. **Aggregation was 169 s/round = the gradient-space DEFENSE metrics.** `compute_gradient_space_metrics`
   (n×n pairwise cosine + top-k energy over the 33M-param flattened update, ×25 clients) is O(n²·d) and useless
   for the clean baseline. Gated behind `log-gradient-metrics` (default true; clean baseline = false) →
   ~150 s/round saved. The cheap per-client update norms still log (the divergent-client signal).
4. **`_load_info` re-unpickled the 580 MB info-cache PER CLIENT.** Memoized on the Task instance (persists per
   Ray-actor process) → each actor reads the pickle once (part of the ~9 s `build_s`).

**Measured (compile-OFF R=2, before the agg fix):** round ≈ 929 s = ~760 s client-training (7 waves × ~104 s:
build 9 s + train ~95 s) + 169 s aggregation. With agg-metrics gated off + memoized loader: **~11–12 min/round
⇒ R=15 ≈ ~3 h** (vs ~4.1 h un-compiled). Trains-clean confirmed (loss dropping, no NaN/OOM). 25-client / 4×A100
= 7 waves; at the single-GPU 244 ms/step the mean shard (1125 kf / batch-4 = 281 steps) is ~69 s/client training,
the largest shard (1449 kf) ~88 s. A 1-client run = single-GPU ~39 min (FL gives up the 4-GPU-DDP+compile that
makes centralized ~7 min/epoch). R=15 submitted (job 6778297) with the optimized config; rounds 1–2 monitored
before letting it run.

---

## Result

Federates locked bb02d on the healthy 25-client `log_group` partition. **Status: PAUSED (owner)** at the decision
of how to lift the non-IID-diluted tail to clear the ≥0.50 floor.

**Headline:**

| run | server-opt | R | peak mAP | NDS | note |
|---|---|---|---:|---:|---|
| FedAdam η=0.01 | fedadam | 15 (timeout@13) | **0.057** | 0.094 | BUG — η under-stepped |
| **FedAvg η=1** | fedavg | **15 (full)** | **0.247** | **0.278** | bug fixed; tail diluted |
| centralized bb02d | — | — | 0.5656 | 0.5733 | the reference |

**The FedAdam η=0.01 bug (found via the owner's 1-client sanity check):** 1-client FL on all 28,130 frames
(iid N=1) — both FedAvg and FedAdam clients trained the identical full epoch (loss ~3.1), but the *global* after
that epoch diverged: FedAvg η=1 → car proxy-recall **0.50** (global = the trained model); FedAdam η=0.01 → **0.00**
(global barely moved). FedAdam normalizes the client delta and steps only ~η/coordinate, so η=0.01 froze the
random-init head. The FL machinery is correct; **η=0.01 was the bug** (D17 itself says "start from a FedAvg
baseline, THEN FedAdam" — η was 10–100× too small).

**FedAvg R=15 per-class (full val, score 0.01 / maxobj 500) — the gap is the non-IID TAIL:**

| class | FL AP@2m | central | retention | recall@tp |
|---|---:|---:|---:|---:|
| car | 0.743 | 0.85 | 87% | 0.95 |
| traffic_cone | 0.556 | 0.72 | 77% | 0.96 |
| pedestrian | 0.606 | 0.80 | 76% | 0.92 |
| barrier | 0.429 | 0.65 | 66% | 0.86 |
| bicycle | 0.146 | 0.42 | 35% | 0.77 |
| truck | 0.107 | 0.48 | 22% | 0.84 |
| motorcycle | 0.130 | 0.68 | 19% | 0.80 |
| bus | 0.041 | 0.53 | 8% | 0.72 |
| construction_vehicle | 0.005 | 0.23 | 2% | 0.51 |
| trailer | 0.000 | 0.22 | 0% | 0.42 |

**Signature:** HEAD classes (present in every client) retain **66–87%**; TAIL classes have **HIGH recall
(0.42–0.84 — the model detects them) but ~0 AP** = detected-but-poorly-calibrated-confidence. Consistent with
genuine non-IID dilution (tail sparse; 6 clients have zero trailer; FedAvg averaging dilutes the tail confidence
signal) + big-batch under-convergence (E=1 full-participation ≈ large batch). mAP climbs r8 0.186 → r15 0.247,
decelerating (+0.002/round) ⇒ a convergence/dilution gap under this recipe, not a bug.

**Reproduce / artifacts:**
- Run: `bash run_fl_bb02d_a100.sh` with `EXTRA_OVERRIDES="server-optimizer=fedavg server-lr=1.0
  server-lr-warmup-rounds=0 server-ema-decay=0.0"`, `TAG=fl_bb02d_fedavg` (config `fl_bb02d_fedadam.json`).
- Snapshots: `…/cycle_04/fl_bb02d_fedadam/fl_bb02d_fedavg/round_{8,10,12,14,15}/final_model.pt` (raw; ema-decay 0).
- Eval: `run_eval_ckpt_a100.sh` with `EXTRA="det-eval-limit=0"` (full val, 0.01/500). FL_TRAINABLE_CHECKSUM
  `7c7639d5…`. NOTE: snapshots lack `provenance.json` (the run completed but the post-run provenance step is in
  the launcher tail; write it before using a snapshot as a D10 reference).

---

## Investigation

Read-only mechanistic diagnosis of the FedAvg-0.247-vs-centralized-0.5656 gap (no platform code change, no new
training), per the owner's directive to understand the MECHANISM, not sweep hyperparameters. Two evidence streams
— a multi-agent literature+theory workflow (`wf_69cb3c68`) and a gradient/module teardown of our own snapshots —
converge.

**TL;DR.** Under our geographic (by-log) non-IID, the shared feature extractor learns fine (recall is high
everywhere) but the per-class CenterPoint heatmap HEAD ends up mis-calibrated for the tail: clients lacking a
class supply only background supervision to that class's channel, FedAvg averages mostly-suppressed channels →
tail "detected but ~0 AP". The FedAdam η=0.01 issue was a separate, already-fixed under-stepping bug (dense
detector gradients → adaptive step collapses to O(η)). The literature flags head/classifier-drift as the
characteristic non-IID failure mode — but warns that **classifier-only fixes are bounded by representation
quality** (the feature space is also distorted), so a full fix is likely two-part.

### Literature: no credible prior FL-BEV/3D-detection-on-nuScenes baseline

Adversarially confirmed: no published, credible, reproducible federated camera+LiDAR BEV/3D detector on nuScenes
under the official 10-class mAP/NDS protocol with an honest FedAvg-vs-centralized table.
- **AutoFed** (MobiCom 2023, arXiv:2302.08646) — the most credible peer-reviewed federated multimodal AD detector
  — is deliberately camera-less (LiDAR+radar), self-collected data, single-class BEV AP. Not a comparator; its
  design targets the same missing-class non-IID we diagnose.
- **FedM2former** (Processes/MDPI 2025, 71.2 mAP) and **BEV-FePNet/DP-DeceFL** (Neurocomputing 2024, ~71.6 mAP)
  nominally match but report mAP above centralized BEVFusion SOTA (~68–70) — internally implausible, FL recipe
  undisclosed, paywalled. Red flags, not baselines.
- The "FedAvg ≈ centralized on nuScenes (56→60)" result is 2D camera detection (arXiv:2509.01868), a
  small-model/mild-partition artifact — not evidence about a 33M-param 3D detector.
- Honest comparators (different domain) reproduce our signature: Fed3D (indoor RGB-D 3D det, arXiv:2604.15795)
  −14–15 mAP@0.25 + −22 AR@0.5 under non-IID; FedPylot (2D YOLO, geographic non-IID, arXiv:2406.03611) gap widens
  with long-tail (10-class 47.8 → 23-class 28.3) and finds large server momentum β=0.9 destabilizes non-IID
  detection; FedDrive (AD segmentation) — the dominant lever is normalization-stat handling.

⇒ As far as the literature shows, our honest FedAvg-0.247 / centralized-0.5656 with per-class retention is the
first credible such comparison (novelty is real).

### Gradient / module teardown (read-only, on snapshots rounds 8/10/12/14/15)

**Per-module update across rounds (FedAvg, relative ‖Δglobal‖/‖W‖):**

| module | r8→10 | r10→12 | r12→14 | r14→15 |
|---|---:|---:|---:|---:|
| camera_backbone (Swin-T) | 0.0038 | 0.0020 | 0.0008 | **0.0003** |
| camera_neck | 0.058 | 0.031 | 0.013 | 0.004 |
| view_transform | 0.076 | 0.038 | 0.015 | 0.005 |
| lidar_encoder | 0.046 | 0.021 | 0.008 | 0.002 |
| lidar_backbone | 0.114 | 0.063 | 0.027 | 0.008 |
| fusion | 0.089 | 0.044 | 0.018 | 0.005 |
| bev_neck | 0.112 | 0.058 | 0.024 | 0.007 |
| **head** | **0.141** | 0.074 | 0.031 | 0.009 |

- The camera backbone barely moves (pretrained + lr×0.1); FL learning concentrates in the LiDAR backbone + BEV
  neck + fusion + head.
- Every module decelerates ~16–20× over the run, tracking the cosine LR anneal (per-client update-norm fell
  36→2.2 from r2→r15 ≈ the 20× LR decay). The cosine-over-15-rounds annealed the LR to ~0 before convergence —
  training effectively slowed while the model was still improving (loss still dropping, mAP still climbing). So
  R=15 under this schedule is under-trained by construction; a stretched schedule / more rounds is a real lever
  (distinct from a true optimum).

**FedAdam mis-allocates the update (the deeper reason it hurt, beyond η=0.01).** Per-module ‖Δ‖ at r8→10,
FedAvg vs FedAdam:

| module | FedAvg ‖Δ‖ | FedAdam ‖Δ‖ | FA/FD |
|---|---:|---:|---:|
| camera_backbone | 1.49 | **2.49** | 0.6 |
| head | **2.81** | 1.66 | 1.7 |
| lidar_encoder | 0.46 | 0.27 | 1.7 |

FedAdam's per-coordinate √v̂ normalization equalizes step magnitude → it over-moves the near-frozen pretrained
backbone and under-moves the head — the opposite of what a detector needs. FedAvg's magnitude-following allocates
correctly. Insight: server-adaptive optimization (FedOpt/FedAdam), tuned on dense well-conditioned
classification/LM gradients, is mismatched to a multi-module detector whose modules need very different step scales.

**The tail is a CALIBRATION failure, not a "head that never updates."** Per-class heatmap-weight update (FedAvg
r8→15) is uniform across classes (rel‖Δw‖ ≈ 0.25–0.30 for tail and head: construction_vehicle 0.30, motorcycle
0.28, bus 0.30 vs car 0.28, pedestrian 0.20), and all heatmap biases stay at the −2.19 focal init (car
−2.19→−2.19, trailer −2.22→−2.20, CV −2.18→−2.15). So the tail head updates as much as head classes — yet tail
AP≈0 with high recall (0.42–0.84) ⇒ a precision/ranking failure, not a missing representation. Per-client
update-norm CoV is only ~0.08 every round (mean 15.6→36→…→2.2; CoV 0.07–0.12) ⇒ the 25 clients are
magnitude-homogeneous; the open question was the angle/cosine between client updates.

### Decisive measurements

**Step 1 — per-class TP-vs-FP score separability (FREE, on saved round-15 predictions).** Matched the saved
`det_eval/results.json` predictions to GT (≤2 m, greedy by score); separability AUC = P(TP score > FP score):

| class | n_pred | recall@2m | TP score md | FP score md | sep-AUC |
|---|---:|---:|---:|---:|---:|
| car | 672k | 0.97 | 0.54 | 0.07 | 0.97 |
| pedestrian | 904k | 0.93 | 0.47 | 0.07 | 0.96 |
| truck | 255k | 0.86 | 0.20 | 0.07 | 0.86 |
| bus | 83k | 0.73 | 0.17 | 0.07 | 0.86 |
| trailer | 49k | 0.44 | 0.09 | 0.07 | 0.78 |
| construction_vehicle | 48k | 0.52 | 0.11 | 0.06 | 0.82 |
| motorcycle | 119k | 0.81 | 0.19 | 0.06 | 0.90 |
| bicycle | 151k | 0.78 | 0.22 | 0.06 | 0.89 |

Findings: (a) sep-AUC is HIGH for every class (**0.78–0.97**) ⇒ NOT a rank-inversion; per-class score calibration
(NorCal / temperature) is empirically a no-op for nuScenes AP (rank-invariant). (b) The tail failure is confidence
COMPRESSION + over-prediction: tail TP scores are low and nearly equal to FP (trailer 0.09 vs 0.07; truck 0.20 vs
0.07) vs head TP≫FP (car 0.54 vs 0.07), and the tail is over-predicted 28–83× ⇒ tail TPs are correct but buried,
low-confidence, in a flood of near-equal FPs ⇒ precision (hence AP) collapses despite high recall. (c) The decent
sep-AUC means the BEV features retain tail signal (backbone OK) ⇒ head retraining on balanced data (cRT, below)
looked promising and the "bounded by representation" caveat looked likely-mild here. ⇒ the fix should
SHARPEN/raise tail confidence, not re-score post-hoc.

**Step 2 — per-client gradient conflict (job 6778494, round_8 global, 25 clients × full local epoch).**
Per-module cross-client cosine (mean | %neg): lidar_encoder 0.671|0%, fusion 0.287|0%, head 0.232|0%, bev_neck
0.191|0%, lidar_backbone 0.142|0%, view_transform 0.199|0%, camera_neck 0.088|0%, camera_backbone 0.013|0.7%.
Per-class heatmap sign-agreement: car 0.78, truck 0.67, bus 0.74, trailer 0.67, CV 0.76, ped 0.71, moto 0.77,
bicycle 0.77 (all ≫ random 0.5).

Finding — **REFUTES the gradient-conflict hypothesis.** No destructive conflict: every module has all-positive
cross-client cosine (**%neg≈0**) and the tail heatmap channels do NOT sign-cancel (0.67–0.77, ≈ head). The 25
non-IID clients agree in direction ⇒ conflict-mitigation (SCAFFOLD / gradient surgery) is NOT indicated. The
tail's agreement is the weaker end (per-class cosine trailer 0.33 vs car 0.50) = a weak, scarce-but-consistent
tail signal.

### Revised unifying mechanism (after all 3 measurements)

The tail collapse is **NOT client conflict — it is tail-specific confidence UNDER-SHARPENING of a scarce,
weak-but-consistent signal.** Each client has few tail instances → weak (consistent-direction) tail gradient; in
this R=15 run, FL big-batch averaging + per-round optimizer reset + LR annealing to ~0 by round 15 did not sharpen
the tail heatmap → tail confidence stays low/compressed (step 1: TP≈FP) → AP collapses despite high recall. The tail is already the
weakest class centrally (trailer 0.22, CV 0.23); FL pushes it below the usable-confidence floor. **Features are
fine** (sep-AUC 0.78–0.86) ⇒ the failure is head-confidence, not representation. This supersedes the earlier
"direction-conflict" framing with a convergence / effective-signal framing; the under-convergence lever (H2) is
promoted and becomes tail-specific (head classes have abundant per-client signal → sharpen fast; tail does not).

Indicated fixes at this point (ranked): (1) cRT head-retraining on balanced data (decisive probe, next section);
(2) FedAvgM server momentum (small β) — accumulates the consistent-but-weak tail signal across rounds (NOT FedAdam
normalization, NOT conflict methods); (3) local class-balanced focal / FedVLS vacant-class KD / more rounds with a
stretched schedule. NOT indicated: SCAFFOLD/gradient-surgery (no conflict), per-class score calibration
(rank-invariant no-op).

### Publishable angle (assessment)

The contribution is NOT a new FL algorithm but: (1) the first credible, reproducible clean federated
camera+LiDAR 3D-detection benchmark on nuScenes with an honest per-class FedAvg-vs-centralized degradation table;
(2) a mechanistic diagnosis localizing the non-IID failure to per-class detection-head confidence-dilution
(per-module cosine + per-class sign-agreement + cRT probe), adapting the FL long-tail/classifier-calibration line
(CCVR/cRT/NorCal/FedRS/FedVLS) to dense 3D detection where it is currently untested; (3) evidence on why
server-adaptive optimization (FedAdam) fails on dense detectors (gradient-density regime). All three are within
reach with diagnostics on the existing model — before any attack/defense work — and de-risk the whole thesis.

---

## cRT probe

The decisive head-vs-representation test for the FedAvg-0.247 tail collapse: freeze the FL-converged feature
stack, balanced-retrain ONLY the head, eval full-val at the 0.247-baseline protocol. Job **6781365** (single
A100, 1h47m, 2026-06-27). Scripts: `scripts/p3_crt_probe.py` + `scripts/run_p3_crt_probe.sh`.

**Setup (designed so a flat tail implicates representation, not a tuning artifact):**
- *Init:* FedAvg round_15 global (`fl_bb02d_fedadam/fl_bb02d_fedavg/round_15/final_model.pt`, the 0.247 model).
- *Freeze:* everything except `head` (0.45% of params, 15 tensors); frozen children pinned `eval()` (deterministic
  frozen features — BN-free Swin+GroupNorm, no DropPath/dropout in the frozen path).
- *Head retrain:* pooled train (28,130 kf), CBGS 1.44× (tail repeat 1.56–1.65) + tail-weighted focal
  (class-weights 1.3–2.5×) + AdamW lr 3e-3 OneCycle (the centralized peak — rules out under-stepping) + EMA 0.999,
  2 epochs (loss 1.895→1.843, converged; head classes already regressing ⇒ not under-trained).
- *Eval:* `t4_readiness_eval --diagnostic`, full val (6019 kf), score 0.01 / maxobj 500 — SAME as the baseline.

**Result — mAP 0.2677 (raw == ema); the tail did NOT recover under this probe:**

| class | FedAvg base | cRT | central | cRT−base | cRT/central |
|---|---:|---:|---:|---:|---:|
| car | 0.743 | 0.661 | 0.85 | −0.082 | 78% |
| barrier | 0.429 | 0.371 | 0.65 | −0.058 | 57% |
| traffic_cone | 0.556 | 0.540 | 0.72 | −0.016 | 75% |
| pedestrian | 0.606 | 0.599 | 0.80 | −0.007 | 75% |
| truck | 0.107 | 0.095 | 0.48 | −0.012 | 20% |
| bus | 0.041 | 0.051 | 0.53 | +0.010 | 10% |
| trailer | 0.000 | 0.012 | 0.22 | +0.012 | 5% |
| construction_vehicle | 0.005 | 0.010 | 0.23 | +0.005 | 4% |
| motorcycle | 0.130 | 0.177 | 0.68 | +0.047 | 26% |
| bicycle | 0.146 | 0.161 | 0.42 | +0.038 | 38% |
| **mAP** | **~0.276** | **0.2677** | **0.566** | −0.008 | 47% |
| NDS | — | 0.2986 | 0.573 | — | — |

(raw checksum `95c447d2`; ema `d29c1adc`; both eval to identical AP to 3 dp.)

**Interpretation — the cRT probe points to a representation-level cause.** Given the head the maximum balanced
signal it could get, the tail moved only marginally off the floor (trailer 0→0.012, CV 0.005→0.010, bus 0.041→0.051 — still
**4–10% of centralized**) and ONLY by trading away head-class AP (car −0.08, barrier −0.06) ⇒ a tail↔head
reshuffle with **no net gain** (mAP −0.008). The head retrained (loss dropped, tail off-floor, head regressed) —
it learned what it could from these frozen features. **Reading under this probe/recipe:** the bottleneck sits in
the frozen non-IID FL representation rather than the classifier head, so head-only fixes (cRT / federated-cRT /
CReFF / NorCal) were INSUFFICIENT here. This matches the investigation's adversarial caveat (CCVR's own limitation
+ RUCR TIFS'24: head fixes are bounded by representation quality) and is the stronger result: non-IID damage in
dense 3D detection looks representational, unlike the 2D-classification long-tail line where cRT usually recovers
most of the gap. (Stated as a probe finding, not a closed law — open to revisiting on Arrhenius, with more rounds,
or with the levers below.)

**Rigor note / cheap follow-up.** The baseline comparison uses the recorded full-val per-class table (mean ~0.276;
the FL in-run headline was 0.247). To make the cRT−baseline delta airtight (identical decode + eligible-set),
re-eval round_15 with the SAME `t4_readiness_eval` (~25 min single A100). The qualitative reading (tail at 4–10%
of central ⇒ not recovered under this probe) does not depend on it.

---

## Next levers (path to ≥0.50 mAP — open; PAUSED at the owner's decision)

The cRT probe steers away from head-only fixes and toward the representation. The remaining levers, with the
guaranteed-floor pragmatic option kept orthogonal:

- **Lever 1 (cRT / head-only) — set aside as a sufficient fix under this probe.** Keep as a published negative
  (the head-vs-representation decoupling result). Worth re-checking if a future representation lever changes the
  frozen features it operates on.
- **Lever 2 — FedAvgM (small β) + more rounds (representation-level).** Let the FEATURES keep learning the tail —
  R=15 under-converged (LR→0 by r15) — and accumulate the consistent-weak tail signal INTO the representation
  across rounds. (Note FedPylot's caution that large β=0.9 destabilizes non-IID detection ⇒ small β.) Distinct
  from FedAdam normalization and from conflict methods.
- **Lever 3 — training-time tail repair during FL.** FedVLS vacant-class KD (preserve tail-class features on the
  6/25 clients with zero trailer, etc.) + class-balanced local focal — fix the tail at the representation source.
  Optionally FedYogi if keeping adaptivity (resists v_t blow-up under heterogeneity); if keeping FedAdam at all,
  η~1.0, τ 1e-2…1e-1.
- **Pragmatic, guaranteed-≥0.50 — warm-start FL from the 0.5656 centralized model.** Init the FL global from the
  centralized ckpt, federate, and measure RETENTION (the kickoff's centralized→FL framing). Cheapest (a
  `server_app` init-from-checkpoint change), realistic AD-FL, reliably ≥0.50 — orthogonal to the scientific
  from-scratch fix; can be reported alongside it.
- (A from-scratch FedAvg at R=30 alone is expected to land only ~0.30–0.35 given the decelerating curve — the
  least likely single lever to clear 0.50 on its own.)
