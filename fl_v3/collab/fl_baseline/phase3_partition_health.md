# MCR Phase-3 STEP 2 — client-partition health + non-IID characterization

> Source of truth for the FL client partition the clean bb02d FL baseline federates. Generated by
> `fl_v3/scripts/p3_partition_health.py` on the **`info_cache_msweep10` trainval `train`** cache (the exact
> data the FL run uses). Machine-readable: `partition_health.json` (this dir).
> **VERDICT: GO** — the partition is healthy; the non-IID is genuine and preserved.

## Config (the EXACT FL partition)

`log_group`, floor=`min-keyframes-per-client`=400, `nuscenes-num-clients`=25, seed=20259 (partition-seed
`""` → falls back to run seed 20259). Cache hash `cb9818e73c1d…`. This is the partition `t4_reference.json`
/ `p1_bb02d.json` declare (`partition-mode=log_group`, N=25, floor=400), so it satisfies the D10 readiness
contract.

## Totals + N-derivation (non-arbitrary floor)

- **50 logs**, **28,130 keyframes**, **944,881 boxes**. Logs/location: boston-seaport 22, singapore-onenorth
  17, singapore-queenstown 7, singapore-hollandvillage 4.
- `n_max@floor400 = 39` ⇒ requested N=25 is **honored** (LPT-balanced into exactly 25 location-coherent
  groups). Floor band: 200→44, 400→39, 600→29 (the floor is not on a cliff).

## Health gates (all PASS)

| gate | result |
|---|---|
| Determinism (re-build → identical shards) | ✅ |
| Fair-comparison anchor (∪ clients == pooled train, 28130) | ✅ |
| No cross-client overlap | ✅ (0) |
| Whole-log integrity (no log split; all 50 logs assigned) | ✅ |
| No starved client (< floor 400) | ✅ (min 812) |
| N == requested (25) | ✅ |

The union of the 25 clients **equals** the centralized pooled-25-client trainval set bb02d was trained on →
**FL-vs-centralized is the same data** (the fairness precondition).

## Per-client volume + continuity (balanced, coherent)

Keyframes per client: **min 812 / max 1449 / mean 1125 / CoV 0.153 / max-min ratio 1.78**. Each client is
**1–4 whole contiguous logs (20–36 scenes)** = real driving sessions, not scattered keyframes. Low volume CoV
⇒ num-examples (keyframe) FedAvg weighting ≈ uniform; no single client dominates the average.

## Non-IID profile (DESIRED — this is the realism the attack/defense story needs)

- **Geographic density axis (the dominant non-IID):** Boston (15 clients) is dense (~32–58 obj/kf); Singapore
  (10 clients: onenorth 5, queenstown 3, hollandvillage 2) is sparse (~14–31 obj/kf). Singapore clients carry
  the largest JSD-to-global (up to 0.143).
- **Label-distribution skew (per-class CoV of per-client counts):** car 0.42 (most uniform) · pedestrian 0.61
  · truck 0.60 · bicycle 0.59 · bus 0.54 · motorcycle 0.68 · construction_vehicle 0.77 · traffic_cone 0.80 ·
  barrier 0.85 · **trailer 1.09 (most skewed)**. Rare/large classes are the most heterogeneous — expected.
- **JSD-to-global (class distribution):** mean 0.053 bits, min 0.010, max 0.143 → **moderate** label skew
  (not pathological). FedAdam server momentum is the lever to recover retention under this skew.
- **Weather/time axis (from `scene.json`):** night clients 3 (15/16/24, all Singapore), rain clients 9, pure
  day-dry 15 — a natural domain-shift axis available for the later attack/defense non-IID story.

## Global (pooled) class distribution — the difficulty map

car 43.7% · pedestrian 19.7% · barrier 13.2% · traffic_cone 8.7% · truck 7.7% · trailer 2.2% · bus 1.4% ·
construction_vehicle 1.3% · motorcycle 1.1% · bicycle 1.0%. The long tail (trailer/bus/CV/moto/bicycle, each
~1–2%) is exactly the set of classes bb02d is weakest on centrally — and the most non-IID across clients.

## Degenerate-class note (a NOTE, not a no-go)

6 clients have **zero instances of a class**: trailer absent from clients 15,17,18,22,23,24; client 24 also
has zero construction_vehicle — **all Singapore** (trailer is a Boston-concentrated class). This is **genuine
geographic rarity, NOT degeneracy**: every client has all head classes (car/ped/barrier/cone/truck), so every
client trains a meaningful local model; the shared CenterPoint heatmap head still receives trailer/CV
gradients from the 19 clients that have them, and FedAvg propagates trailer features globally. We **preserve**
this skew (do NOT IID-ify). It is flagged so the later per-class FL retention is read with this in mind (a
small extra FL hit on trailer/CV beyond the centralized gap is expected and attributable to this skew).

## Decision

**GO.** No partition fix needed (the floor=400 / N=25 derivation already yields a balanced, coherent,
non-degenerate partition). Proceed to STEP 3 (recipe). The partition is deterministic and D10-compliant, so it
can host the clean FL reference T5 binds to.
