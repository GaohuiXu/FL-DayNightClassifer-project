# Cycle 02 pivot, Phase D2, cell 7/9 — pretrained ResNet18, head-only FT, no attack
# Calibration cell for the head_only regime. KEY RISK: clean acc may tank
# because the random 3x3 conv1 + ImageNet bn1 stats produce a feature
# distribution mismatch that the head alone cannot fully fix.
# If clean_acc < 0.60 after 100 rounds, queue the image-size=64 fallback.
experiment-name: cycle02-pretrained-headonly-clean
cycle: cycle_02
phase: phaseD2
model-type: resnet18
pretrained-init: true
trainable-layers: head_only
num-clients: 50
num-server-rounds: 100
fraction-train: 1.0
min-available-nodes: 50
min-train-nodes: 2
num-local-epochs: 3
learning-rate: 0.05
lr-schedule: cosine
lr-min: 0.0001
attack-type: none
defense-type: none
checkpoint-rounds: "0,5,10,25,50,75,100"
output-dir: '/mimer/NOBACKUP/groups/naiss2024-22-991/gaohui/fl_outputs/gtsrb'
wandb-group: cycle02-pretrained-headonly
wandb-tags: pretrained-pivot
